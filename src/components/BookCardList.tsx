import React from 'react';
import './BookCardList.scss';

const BookCardList: React.FC = () => {
   // Assuming you have a `books` array in your search state
 
  return (
    <div className="book-card-list">
      
    </div>
  );
};

export default BookCardList;
